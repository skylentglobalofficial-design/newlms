# SQL analysis — Northwind sales

Four reproducible queries (duplicates, category, region, month) plus interpretation.

Paste SQL and result tables into the submission box. The LMS will not run them.

Use the same valid-row rule as the spreadsheet assignment so a reviewer can reconcile the two artifacts.
