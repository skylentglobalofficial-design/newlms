# Supabase PostgreSQL connection string (pooler or direct)
DATABASE_URL=

# Session signing secret (reserved for future cookie signing)
SESSION_SECRET=

# Set true when serving over HTTPS (production / public preview)
COOKIE_SECURE=true

# Explicit demo auth fallback for development only
VITE_DEMO_MODE=false
